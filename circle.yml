machine:
  pre:
    - cd /opt/circleci/.pyenv; git pull
  python:
    version: 3.6.0

dependencies:
  override:
    - pip install Django
    - make deps

test:
  override:
    - python manage.py test
